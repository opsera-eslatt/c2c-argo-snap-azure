apiVersion: v1
kind: Namespace
metadata:
  name: c2c-snap-azure-ns
  
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: c2c-snap-azure-deployment
  namespace: c2c-snap-azure-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: back-end
          image: eslattacr.azurecr.io/azure-demo-push-repo:53bd791370488bc597c1342a3664c090e97133a6

---
#Service
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: c2c-snap-azure-label
  name: c2c-snap-azure-service
  namespace: c2c-snap-azure-ns
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: web
  sessionAffinity: None
  type: LoadBalancer

---